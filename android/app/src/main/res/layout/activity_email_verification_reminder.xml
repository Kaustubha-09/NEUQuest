<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:padding="24dp"
    android:background="@color/background_color">

    <TextView
        android:id="@+id/message_text_view"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="120dp"
        android:text="Please verify your email address before logging in."
        android:textColor="@color/text_color"
        android:textSize="32sp"
        android:textStyle="bold" />

    <TextView
        android:id="@+id/additional_message_text_view"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@id/message_text_view"
        android:layout_marginTop="16dp"
        android:text="To ensure your account's security, please verify your email. Check your inbox for the verification link."
        android:textColor="@color/text_color"
        android:textSize="14sp" />

    <Button
        android:id="@+id/resend_verification_button"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@id/additional_message_text_view"
        android:layout_marginTop="24dp"
        android:text="Resend Verification Email"
        style="@style/CustomButton"
        android:paddingHorizontal="24dp"
        android:paddingVertical="12dp"
        android:textSize="16sp" />

    <Button
        android:id="@+id/back_to_login_button"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@id/resend_verification_button"
        android:layout_marginTop="16dp"
        android:text="Back to Login"
        style="@style/CustomButton"
        android:paddingHorizontal="24dp"
        android:paddingVertical="12dp"
        android:textSize="16sp" />

</RelativeLayout>